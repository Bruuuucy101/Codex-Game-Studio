# NPC Voice Implementation Plan

> For agentic workers: use superpowers:subagent-driven-development task gates.

**Goal:** Optional baked NPC speech with stable voice identities, safe per-line reuse and playable output mapping.
**Spec:** docs/superpowers/specs/2026-09-21-npc-voice.md
**Architecture:** Strict voice batch planner over the preceding shared asset transport, journal and artifact publication primitives; synchronous ElevenLabs adapter; small manifest consumer and coordinator-owned workflow.
**Tech Stack:** Python3.10 standard library and real local HTTP/PCM fixtures.

## Global Constraints

- Preserve original417-file provenance and all existing studio identities. Extend canonical workflow sources and regenerate wrappers; never edit generated content directly.
- Python3.10+ standard library, reuse existing asset HTTP/journal/path/publication helpers where their exact interfaces fit. Do not require Pillow for WAV-only commands. No duplicate generic transport/journal framework.
- Fixed HTTPS api.elevenlabs.io, credentials only ELEVENLABS_API_KEY via xi-api-key. No secrets in distributed clients, logs, requests on disk, or artifacts. No global config changes or live paid calls during implementation.
- Strict project-confined JSON inputs, source hashes, no symlink/traversal or output overwrites. Private state under ignored .ccgs-assets/voice/, restrictive permissions. Default plan/bake/collect operations have no writes/network unless explicitly selected.
- Each line is a synchronous journaled submission. Persist intent before POST; never automatically retry any uncertain POST. Completed audio is durable before marking generated. Uncertain lines remain blocked; successful other lines remain usable. Local coordination does not imply remote exactly-once semantics.
- Evidence must distinguish fixture HTTP and valid audio decoding from live provider quality, listening approval, and actual game-engine import.

### Task 1: Durable voice generation and playback manifest

**Files:** Create tools/ccgs/voice/{request,elevenlabs,batch,audio,cli,__init__}.py, tests/voice_tools/, docs/codex-adapter/npc-voice.md, examples/npc-voice/; extend tools/ccgs/cli.py, optional shared asset primitives only as needed, CI blocking test job, README, canonical .claude/skills/npc-voice/SKILL.md and behavior-testing catalog/docs. Update generated files/patch ledger through repository tools.

- [ ] Read binding spec and preceding asset Task1report for actual helper signatures. Inspect official saved ../round3-research/elevenlabs-openapi.json for exact schema. Existing localize VO/key/path conventions and audio role boundaries are mandatory.
- [ ] Write failing request/HTTP/journal/PCM tests for spec cases before implementation. Cover canonical source requirements and explicit standalone, exact text/no unresolved placeholders, duplicate/malformed IDs/settings, model/language/voice eligibility with full pagination and bounded cursors, no network/write dry mode.
- [ ] Implement strict planner and bounded read-only discovery; support only the documented initial request subset. Expose plan/bake/status/collect/voices/models/verify CLI. No unknown fields or silent model substitutions. Missing service remains optional.
- [ ] Implement per-line durable synchronous operation using existing shared low-level primitives; no fake async task and no blind POST retries. Keep semantic line hash separate from batch source revision so source-only change reuses unchanged audio with honest provenance. Add explicit generation_revision for intentional paid regeneration. Test crash/uncertain/mixedfailure/concurrent paths via real HTTP counts and actual CLI subprocesses.
- [ ] Fully validate PCM WAV at24kHz16bit, positive complete frames,1or2channels and bounded size/duration; test wrongcodec/rate/truncated chunks. Publish create-only retained-stage ownership and immutable complete manifest last. Preserve collisions/changedownedfiles; partial batch has no complete manifest.
- [ ] Add engine-neutral manifest loader example and real twoNPC/multipleline fixture acceptance, unchanged0POST and changedline1POST; no fake listening or engine-import assertion. Add coordinator-owned npc-voice workflow, actual role handoffs/full-lean-solo fallback, generation→listening→import→localize-validation states. Add behavior specification that preserves original limitations.
- [ ] Run focused voice suite then assets/board/core suites, generator/strict provenance/diff checks. CI voice job requires no paid account or Pillow; retain prior release gates. Self-review/commit owned files and report RED/GREEN evidence, exact interfaces, workflow count and remaining live gaps. No children/publication/paid calls.
