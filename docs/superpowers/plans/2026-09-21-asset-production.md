# Asset Production Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Optional executable PixelLab, Meshy and Tripo production with durable recovery and verified project-local artifacts.

**Architecture:** Small shared request/HTTP/state/artifact modules plus explicit provider adapters. PixelLab establishes both synchronous and asynchronous paths;3Dproviders reuse those interfaces. Native MCP and NPC voice workflows integrate later through the tested artifact boundary.

**Tech Stack:** Python3.10+ standard library; optional Pillow12.3.0; unittest plus local HTTP fixtures and actual CLI subprocesses.

**Spec:** `docs/superpowers/specs/2026-09-21-asset-production.md`

## Global Constraints

- Preserve all existing studio capabilities, original417-file lock, reviewed patch provenance and generated entry consistency. Do not edit generated wrappers directly.
- Python3.10+; core studio commands remain standard-library-only. Asset image decoding uses optional `Pillow==12.3.0` in `requirements-assets.txt`, imported lazily. No global installs, implicit model downloads, credential collection or host configuration changes.
- Production API origins are fixed official HTTPS hosts. Credentials come only from named environment variables: `PIXELLAB_SECRET`, `MESHY_API_KEY`, `TRIPO_API_KEY`. Never persist/print API keys or authorization headers. Never expose signed URL query strings or raw remote error bodies in public plans, CLI output or logs. Necessary signed input URLs and transient output material may be stored only in restrictive private operation state, never published artifacts or ordinary summaries. No arbitrary API origin/executable is accepted through request/CLI/config. Tests inject transport boundaries in library code only.
- All operation state is confined to ignored `.ccgs-assets/`; receipts contain request/source/artifact hashes, provider operation/task IDs and distinct statuses. Existing project documents and outputs are not overwritten. Dry operations do not contact providers or create state.
- Validate the complete supported request before any billable POST. Persist submission intent first and accepted task IDs immediately. Never blindly repeat an uncertain submission, including after process death. Local locks protect concurrent operations; do not claim provider or cross-host exactly-once semantics.
- Use actual HTTP boundaries in credential-free tests, not only mocked provider methods. Mark fixture evidence separately from live account, visual/audio quality and engine-import evidence. No paid calls are needed to implement or claim local contract tests.
- Downloads use bounded time/bytes, separate credential-free connections and checked redirect destinations. Validate final output paths and content before atomic create-only publication; reject traversal, symlink components, destination collisions and corrupt/mismatched artifacts.

## Review Focus

- Accepted taskID survives restart and unchanged resume makes zero generation POSTs; stateful HTTP request counts prove this.
- Uncertain synchronous or asynchronous submit cannot silently charge again; durable intent/crash/error cases remain blocked until explicit operator action.
- A signed output redirect never receives API credentials or targets an unapproved local/private origin; transport tests inspect actual request headers and targets.
- Valid-magic corrupt images and malformed GLB container/buffer references cannot be published as validated artifacts; real decoder/container fixtures exercise these cases.
- Source edits, directory symlinks and competing destination writers cannot overwrite or misattribute an existing file; wholecollection preflight plus atomic create-only/race tests prove ownership.

### Task 1: Shared production execution and PixelLab V2

**Files:** Create `tools/ccgs/assets/{__init__,request,http,jobs,artifacts,pixellab,cli}.py`, `requirements-assets.txt`, `tests/asset_tools/{test_requests,test_http,test_jobs,test_artifacts,test_pixellab,test_cli,fixtures}.py`, realistic fixtures as needed; extend `tools/ccgs/cli.py`, `.gitignore`, `.github/workflows/codex-adapter.yml`, docs/codex-adapter/asset-production.md and README-CODEX usage. Preserve preceding board-sync changes.

**Interfaces:**
- Request preparation returns a canonical JSON-serializable plan containing validated selected provider/operation/parameters, all input source hashes, expected outputs and request SHA256. The root and private state directory are explicit arguments, never global config.
- A small provider result describes a status, optional taskID, typed JSON data (including private upload results), and named inline-byte or URL artifacts. Non-media operations may complete without public outputs; schema-validated JSON is a supported declared result format. Shared code loads/pins dependency receipts, while adapters perform only typed read-only eligibility checks before their single submission. PixelLab-specific envelopes are decoded only in its adapter. Shared jobs own persistence, locking, source freshness, deadlines and output publication. Expose and document exact final signatures/schema in the report so Task2 and later voice/MCP tasks consume them verbatim.
- Production transport uses fixed-origin HTTPS; test-only library injection can map those origins to a controlled local HTTP server. No CLI/config arbitrary endpoint or executable option. Keep provider authorization entirely separate from artifact download requests.
- Support the spec's `assets plan/submit/status/resume/collect/ingest` command contract, with lazy import and default no-write. Add a read-only `assets balance --provider pixellab` route; this contacts only the documented balance endpoint and does not generate.

- [ ] Read binding spec, canonical asset-spec/art-bible/asset-audit and research `../round3-research/external-integrations.md`. Read `../round3-research/provider-contract-matrix.md` and its exact limitations. Use the downloaded official `../round3-research/pixellab-openapi.json` to derive endpoint-specific required fields, size constraints and response envelopes. Record supported parameter subsets and source hashes/URLs in docs; do not use PR115 wrappers as an API authority.
- [ ] Author meaningful failing unit/real-CLI tests before implementation. Request cases include duplicate/unknown JSON fields, nonfinite/bool dimensions, missing canonical source/spec ID, explicit standalone mode, malformed IDs/paths, bounded source/input bytes, full source hashing including local image/mask parameters, unexpected output keys/formats, absent Pillow before any POST, no-state/no-network preview and existing output collisions before paid submission.
- [ ] Implement request preparation and safe project-local I/O. Use narrow functions for strict JSON, source path/hash validation, no-symlink/no-traversal outputs, owned lock and atomic receipt writes. All image/mask source bytes are freshness-pinned even if not repeated manually in source_files. Reject API keys/auth/host fields rather than copying them into receipts. Add actual content checks for PNG/JPEG and GLB plus owned ingestion receipts; do not claim any engine import.
- [ ] Implement bounded HTTP with JSON/byte limits, optional bounded multipart request bodies for explicit upload operations, safe diagnostics, TLS, explicit timeouts and monotonic deadlines. Add local-server tests for malformed/error envelopes,401/402/422/429/5xx, safe GET retries, mutation single-attempt behavior, redirects, private-network targets, auth isolation, slow/partial/excessive bodies and deadline exhaustion. Test injection stays unavailable through production configuration.
- [ ] Implement durable operation state and CLI. Journal before POST, persist taskID/inline output immediately, distinguish submitting/unknown/pending/generated/collected and remote failures. Rerun sameID+samehash returns state; conflicting hash rejects; resume with a knownID never submits. Handle crash points, response loss, lock contention, atomic state corruption and no-op repeat with actual HTTP counters. Read-only status must not write state.
- [ ] Implement all PixelLab operations declared in spec with exact endpoint-specific payloads and synchronous/async result parsing. Validate local image/mask dimensions and encode documented nested inputs. Add six explicit local background prompt presets without hidden multi-job fanout; document/apply chosen preset as part of the request hash. Cover processing/completed/failed background jobs, last_response image extraction, source/output dimension differences and balance errors. Apply spec square-only UI and no-crop inpaint limits; label their concrete completed-image decoder compatibility/experimental because official shape is unresolved. Unknown result shapes remain recoverable private evidence, never successful collection or a new POST. Requests outside the supported parameter subset fail actionably before a network call.
- [ ] Implement collection all-target preflight, temporary download/decode, full content checks and same-filesystem create-only hard-link publication with retained staged inode+hash ownership. Follow the spec's exact crash reconciliation and conservative unsupported-filesystem behavior; do not use hash-only adoption/rollback. Cover duplicate destinations, alternate-suffix symlinks, concurrent output writers, modified owned artifacts, death after link before receipt, unrelated identical-byte competitor, interrupted collection and committed-owned exact-hash rerun. Persist provenance and leave creative review/import/audit pending.
- [ ] Add a separate assets CI job installing requirements-assets, blocking release alongside all prior jobs. Run focused assets suite, full board suite with its existing optional venv, core suite without optional dependency coupling, strict provenance, generation consistency and diff checks. Preserve original lock; update exact reviewed patch records only for changed baseline sources. Self-review and commit owned files; no child agents or live paid calls. Report RED/GREEN output, exact reusable interfaces, supported operations/parameters and all live acceptance gaps.

### Task 2: Meshy and Tripo3D operation adapters

**Files:** Create `tools/ccgs/assets/{meshy,tripo}.py`, `tests/asset_tools/{test_meshy,test_tripo,test_pipelines}.py`; extend provider registry/CLI/docs, example requests, `artifacts.py` JSON result validation and tests as narrowly needed. Reuse Task1 request/HTTP/jobs/artifact interfaces; preserve PixelLab and board behavior.

**Interfaces:** Consume Task1 report's exact signatures and receipt schema. Each provider adapter validates its documented parameter subset, builds an explicit request, decodes its own provider status/result shape, and supplies named artifacts; it must not duplicate the shared journal/download/locking engine. Models and actions are explicit inputs, never selected by guessing latest or a default animation sweep.

- [ ] Read Task1 report, `../round3-research/provider-contract-matrix.md`, and its official Meshy/Tripo references. Apply the binding spec's exact initial models/action namespaces and source-operation dependency rules, including the free Meshy action-library lookup. Capture operation-specific current required input names, supported model values, task statuses and output shapes before writing each adapter. Meshy uses its documented V1/V2 endpoint mix; Tripo uses V3 dedicated routes only. Resolve any ambiguous or missing official field via primary docs/first-party pinned SDK source and report material conflicts to controller.
- [ ] Write failing stateful local HTTP tests for each declared operation, not only happy-path generation: Meshy preview/refine/image/rig/one-action animation/retexture/remesh; Tripo upload-image/text/image/rig-check/rig/retarget. Assert actual methods/paths/headers/JSON, correct task IDs, status families and named GLB outputs. Unknown parameters, incompatible model/operation combinations, unsupported artifact formats and missing dependent-task evidence fail before submission.
- [ ] Implement the adapters with explicit supported schemas and current provider errors. Authenticate only the API origin; output hosts never get keys. Record provider task IDs, structured safe failure category and actual result type. Account/model entitlement errors are surfaced without substituting another model/provider. A rig-check result is validation data, not a generated mesh; support an explicit JSON result artifact/receipt for that operation without pretending it is GLB.
- [ ] Support explicit image inputs using the documented eligible HTTPS URL or data-URI flow where valid. Tripo local upload is a separate upload-image job with its own ID and private typed result; a later generation job resolves an explicit local input_job reference. Shared code must load/hash/validate that dependency receipt, not let the adapter hide multiple POSTs. Any local upload is previewed, freshness-pinned and bounded; no upload during plan/status. Test crash after upload persistence then exactly one independent generation submit, and uncertainty on each route. Preserve the existing no-blind-submit-retry rule for all POST stages, including uploads with side effects.
- [ ] Document and test restartable preview→refine→rig→one-action and generation→rig-check→rig→retarget pipelines as explicit independent jobs. Restart from saved stageIDs, make zero repeated completed-stage POSTs, stop on failed/ineligible dependencies and never run an unrequested action. Handle expired signed URLs by re-querying a known successful task, not regenerating.
- [ ] Validate collected GLB content and declared companion requirements using real structurally valid/corrupt model fixtures; test multiple/missing outputs and mismatched provider response envelopes. Retain provider/account, visual quality, animation and engine-import evidence as distinct unavailable/pending items unless actually exercised.
- [ ] Update capability/parameter and issue23 scope tables accurately; do not label media/extraction/Blender/GUI complete here. Run all assets tests, board/core suites, strict/generation/diff checks, self-review and commit. Report exact operations, field coverage, RED/GREEN evidence and remaining live limitations. No child agents, paid API calls or publication.
